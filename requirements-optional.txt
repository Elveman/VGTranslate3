# Optional dependencies for VGTranslate3
# Install as needed: pip install -r requirements-optional.txt

# Local OCR (Tesseract + OpenCV)
# pip install -r requirements-local-ocr.txt
pytesseract>=0.3,<0.4
opencv-python-headless>=4.10,<5
numpy>=2.0,<3

# Legacy support (Python 2 compatibility)
# Only needed for older systems
# future>=0.18,<1
